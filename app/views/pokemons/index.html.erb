<%# app/views/records/index.html.erb %>
<%# react_component renders the compnent we want, and we pass in the data through this erb. This is a method from React-Rails GEM! %>
 <%= react_component 'Pokemons', { data: @Pokemons } %>
